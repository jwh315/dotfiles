-- Markdown preview in the terminal using the 'glow' CLI tool
-- Renders markdown files with syntax highlighting and formatting
-- Customization: Lazy-loaded only when :Glow command is used
return {
  'ellisonleao/glow.nvim',
  config = true,
  cmd = 'Glow',
}